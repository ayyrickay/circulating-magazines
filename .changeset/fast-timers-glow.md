---
"circulating-magazines": patch
---

Update project tooling to Node.js 24 LTS.

- Switch GitHub Actions CI to Node 24.
- Add `.nvmrc` for local Node version alignment.
- Declare supported runtime in `package.json` with `engines.node`.
